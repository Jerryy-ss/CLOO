<div class="admin-content"><div class="admin-title"><small>{{ $eyebrow }}</small><h1>{{ $heading }}</h1></div>@if(!empty($alert))<div class="alert-banner">{{ $alert }}</div>@endif<div class="panel"><div class="filterbar" style="margin-bottom:24px"><input placeholder="Cari nama, judul, username, ISBN..."><button class="btn btn-outline">Filter</button><button class="btn btn-outline">Terbaru</button></div><table class="data-table"><thead><tr>@foreach($columns as $c)<th>{{ $c }}</th>@endforeach</tr></thead><tbody>@foreach($rows as $row)<tr>@foreach($row as $cell)<td>{!! $cell !!}</td>@endforeach</tr>@endforeach</tbody></table><div style="display:flex;justify-content:space-between;margin-top:24px;font-size:13px;color:var(--muted)"><span>{{ $footer ?? 'Menampilkan 1-3 dari total data' }}</span><span>Sebelumnya　　Selanjutnya</span></div></div></div>
