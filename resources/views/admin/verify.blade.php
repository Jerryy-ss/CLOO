@extends('layouts.admin') @section('content')<div class="admin-content"><div class="admin-title"><small>Verifikasi Pengambilan</small><h1>Verifikasi Barcode</h1></div><div class="panel"><form method="GET"><div class="field"><label>Kode Barcode</label><input name="kode_barcode" value="{{ request('kode_barcode') }}" autofocus placeholder="Scan atau ketik kode barcode"></div><button class="btn btn-primary">Verifikasi</button></form>@if($loan)<hr><h3>{{ $loan['nama_buku'] }}</h3><p>Peminjam: {{ $loan['nama_peminjam'] }}</p><p>Status: <b>{{ strtoupper($loan['status']) }}</b></p>@if($loan['status']==='disetujui')<form action="{{ route('admin.verify.pickup',$loan['_id']) }}" method="POST">@csrf<button class="btn btn-primary">Konfirmasi Buku Diserahkan</button></form>@endif@elseif(request('kode_barcode'))<div class="notice">{{ $error ?? 'Barcode tidak ditemukan.' }}</div>@endif</div></div>@endsection
