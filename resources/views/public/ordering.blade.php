@extends('layouts.app')
@section('content')
@include('components.header')
<div class="container" style="padding-top:32px;color:var(--muted);font-size:14px"><a href="{{ route('catalog') }}">Katalog</a> › <b style="color:var(--navy)">{{ $book['nama_buku'] }}</b></div>
<div class="order-layout"><div class="detail-card"><div class="detail"><div class="cover"><img src="{{ $book['cover_url'] ?? 'https://www.figma.com/api/mcp/asset/59c36a57-0450-4eb8-b3c4-b94dbae307ef.png' }}" alt="{{ $book['nama_buku'] }}"></div><div><span class="pill">{{ strtoupper($book['kategori'] ?? 'BUKU') }}</span><h1>{{ $book['nama_buku'] }}</h1><div style="color:var(--muted);font-weight:600">Karya {{ $book['penulis'] ?? '-' }}</div><hr style="border:0;border-top:1px solid var(--line);margin:20px 0"><p>{{ $book['deskripsi'] ?? 'Informasi buku belum tersedia.' }}</p><div class="meta"><div><small>Ketersediaan</small><strong>{{ (int)($book['stock_buku'] ?? 0) > 0 ? '● Tersedia' : '● Tidak tersedia' }} ({{ (int)($book['stock_buku'] ?? 0) }} Eks)</strong></div><div><small>Lokasi Rak</small><strong>Perpustakaan</strong></div></div></div></div></div>
<form class="form-card" action="{{ route('ordering.store') }}" method="POST"><h2>Formulir Reservasi</h2>@csrf<input type="hidden" name="id_buku" value="{{ $book['_id'] }}"><div class="field"><label>Nama Lengkap Peminjam</label><input value="{{ session('auth.member.nama') }}" readonly></div><div class="field"><label>Nomor Anggota</label><input value="{{ session('auth.member.id') }}" readonly></div><div class="field"><label>Tanggal Pengambilan Buku</label><input name="tanggal_pengambilan" type="date" min="{{ now()->format('Y-m-d') }}" required></div><div class="notice">Buku yang dipesan akan menunggu persetujuan petugas. Barcode aktif setelah disetujui.</div><button class="btn btn-primary full" {{ (int)($book['stock_buku'] ?? 0) < 1 ? 'disabled':'' }}>Konfirmasi Pemesanan</button></form></div>
@include('components.footer')
@endsection
